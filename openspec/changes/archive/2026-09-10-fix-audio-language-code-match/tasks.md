## 1. Эквивалентность языковых кодов в resolver

- [x] 1.1 Добавить чистый helper приведения языкового тега к одной форме по ISO 639 (двухбуквенная ↔ трёхбуквенная), переиспользуя вендоренный `yt_dlp.utils.ISO639Utils` с тождественным fallback для неизвестных кодов; проверка: direct-вызов helper'а даёт `ru`↔`rus`, `en`↔`eng`, а неизвестный код возвращается без изменений
- [x] 1.2 Применить helper в `resolver._language_matches`, сохранив регистронезависимость, `_` → `-` и совпадение по региону; проверка: в `tests/test_resolver_audio_subtitles.py` новые кейсы `ru` ↔ `rus` и `en` ↔ `eng` проходят, прежний кейс `ru` ↔ `ru-RU` не ломается
- [x] 1.3 Добавить helper формы кода для подсказки плееру (короткая → длинная, только базовый подтег); проверка: для `ru` helper возвращает `rus`, для `en-US` — `eng`, для неизвестного кода — исходное значение

## 2. Подсказка ISA в совпадающей форме

- [x] 2.1 В `default.audio_intent` и `default.play_stream` отдавать в `inputstream.adaptive.original_audio_language` форму кода, совпадающую с манифестом; проверка: shim-тест `tests/test_kodi_shim_audio_subtitles.py` фиксирует `rus` для оригинала `ru`
- [x] 2.2 Оставить `audio_target` равным разрешённому языку для post-start выбора и не менять поведение при `Auto`; проверка: shim-тест — при `Auto` свойство `original_audio_language` не выставляется, `audio_target` пуст

## 3. Надёжный детерминированный выбор после старта

- [x] 3.1 Убедиться, что `resolver.pick_audio_track("ru", [...])` выбирает дорожку `rus`, а не `en-US`; проверка: desktop-тест на списке языков `['en-US', 'en-US', 'rus', 'rus']` возвращает индекс одной из `rus`
- [x] 3.2 Проверить, что `player_monitor.select_audio_track` реально переключается на `rus` и не прерывает воспроизведение при отсутствии совпадения; проверка: shim-тест с fake player — переключение происходит, а при несовпадении возвращается `None` без исключений

## 4. Тесты и статические проверки

- [x] 4.1 Прогнать линтер и типы; проверка: `ruff check .` и `mypy .` завершаются без ошибок
- [x] 4.2 Прогнать все тест-скрипты; проверка: `python tests/test_resolver_audio_subtitles.py`, `python tests/test_kodi_shim_audio_subtitles.py` и остальные `tests/*.py` проходят

## 5. Проверка на живом Kodi

- [ ] 5.1 На русском видео с авто-дубляжом убедиться, что воспроизведение стартует на оригинальной русской дорожке; проверка: в `kodi.log` открыт поток `acont=original:lang=ru`, английский дубляж `dubbed-auto:lang=en-US` не играет, а строка `no audio track for 'ru'` больше не появляется
- [ ] 5.2 Убедиться, что при `Auto` поведение прежнее и недоступная целевая дорожка не блокирует воспроизведение; проверка: воспроизведение стартует без подсказки/переключения и без ошибок
