## 1. Резолвер: корректная детекция оригинала и чистый выбор дорожки

- [x] 1.1 Переписать `resolver.original_audio_language`: оригинал определяется по `language_preference == 10` или маркеру `(original)` в `format_note`; `(default)`/`language_preference == 5` — только слабый fallback, когда явного оригинала нет; при отсутствии маркеров и единственном языке — этот язык; иначе `None`. Проверка: `python3 tests/test_resolver_audio_subtitles.py`
- [x] 1.2 Обновить кейсы `1.2` в `tests/test_resolver_audio_subtitles.py`: убрать ожидание «`(default)` — это оригинал» и добавить кейс «дорожка помечена default (дубляж) + отдельная дорожка с `(original)`/`language_preference == 10` → выбран оригинал». Проверка: тест зелёный
- [x] 1.3 Добавить чистый xbmc-free helper выбора дорожки (например, `resolver.pick_audio_track(target_lang, tracks)`): нормализация языка (регистр, `_`→`-`, совпадение по базовому подтегу `ru` ↔ `ru-RU`), игнорирование служебных дорожек (descriptive/`desc`), предпочтение роли «оригинал», затем AAC-LC перед HE-AAC среди дублей; возврат индекса или `None`. Проверка: новые кейсы в `tests/test_resolver_audio_subtitles.py`
- [x] 1.4 Добавить чистый helper, превращающий выбранный язык/режим в целевой язык для сопоставления (учёт `Original` → детектированный оригинал, явный тег → сам тег, `Auto` → ничего). Проверка: кейсы в `tests/test_resolver_audio_subtitles.py`
- [x] 1.5 Прогнать `python3 tests/test_resolver_quality.py` — убедиться, что правки резолвера не задели выбор качества

## 2. Роутер и наблюдатель: детерминированный выбор после старта

- [x] 2.1 В `default.py` вычислять целевое намерение по аудио (mode/lang) в одном месте и передавать его в запуск наблюдателя (`start_monitor`), не дублируя логику. Проверка: shim-тест `tests/test_kodi_shim_audio_subtitles.py`
- [x] 2.2 Расширить `player_monitor`: после старта воспроизведения однократно получить доступные аудио-потоки плеера, выбрать целевой через `resolver.pick_audio_track` и переключиться на него; чтение потоков и переключение изолировать в тонком адаптере, а выбор — оставить чистой функцией. Проверка: fake-плеер в shim-тесте управляет выбором
- [x] 2.3 Обеспечить fallback и идемпотентность: при отсутствии совпадения, недоступном API или исключении — только лог и продолжение воспроизведения; после успешного переключения повторно не переключать. Проверка: shim-кейсы «нет совпадения» и «API недоступен» не роняют playback
- [x] 2.4 Сохранить подсказку ISA `inputstream.adaptive.original_audio_language` как первый шаг и логировать целевой язык, доступные дорожки и фактически выбранную. Проверка: shim-тест видит свойство и запись в логе
- [x] 2.5 Обновить docstring `default.py` (аудио теперь применяется и после старта). Проверка: `python3 tests/test_kodi_shim.py`

## 3. Тесты поведения

- [x] 3.1 В `tests/test_kodi_shim_audio_subtitles.py` добавить сценарий multiaudio: оригинал `ru` и дубляж `en`, каждый в двух кодеках; при `Original` итоговый выбор — русский (не английский), при `en-US` — английский. Проверка: `python3 tests/test_kodi_shim_audio_subtitles.py`
- [x] 3.2 Добавить shim-кейсы `Auto` (переключения нет) и «целевого языка нет среди дорожек» (выбор плеера сохраняется, ошибок нет). Проверка: тот же тест
- [x] 3.3 Прогнать связанные наборы и убедиться, что ничего не сломано: `python3 tests/test_desktop.py && python3 tests/test_kodi_shim.py && python3 tests/test_kodi_shim_audio_subtitles.py && python3 tests/test_kodi_shim_quality.py && python3 tests/test_kodi_shim_list.py`

## 4. Линтеры и типизация

- [x] 4.1 `ruff check .` — без замечаний (baseline уже зелёный: `All checks passed!`). Проверка: `ruff check .` завершается с кодом 0
- [x] 4.2 `ruff format` для файлов, изменённых этим change (`default.py`, `resources/lib/resolver.py`, `resources/lib/player_monitor.py` и затронутые тесты), затем `ruff format --check <эти файлы>` — зелёный. Учесть baseline: `ruff format --check .` уже падает на 21 файле, не связанном с change, поэтому массовое форматирование всего репозитория — вне scope (отдельное решение). Проверка: `ruff format --check <изменённые пути>` завершается с кодом 0
- [x] 4.3 `mypy .` — без ошибок (baseline зелёный: `Success: no issues found in 6 source files`; учитывая `exclude` для `resources/lib/yt_dlp` и `tests/` из `pyproject.toml`). Проверка: `mypy .` завершается с кодом 0

## 5. Спайк DASH и runtime-верификация на реальном Kodi

- [ ] 5.1 Спайк: определить рабочий плеер-API выбора аудиодорожки в установленной версии Kodi/ISA (доступные потоки, язык/имя, переключение) и зафиксировать результат в `design.md` (обновить D2). Проверка: запись в `design.md` с фактическим API и версией
- [ ] 5.2 Спайк DASH/MPD: проверить, даёт ли MPD по одной аудио-дорожке на язык (2 записи вместо 4 по кодекам) и соблюдает ли выбор языка; зафиксировать в `design.md` (D4). Проверка: запись в `design.md`; при выигрыше — предложение отдельного change
- [ ] 5.3 Runtime-верификация на видео «русский оригинал + английский dubbed-auto, каждый в AAC-LC и HE-AAC»: при `Original` играет русская дорожка, а не английская; при явном `en-US` — английская; тишины и «прыжков» дорожки нет. Проверка: наблюдение на реальном Kodi, результат отмечен в `design.md`
- [ ] 5.4 Проверить, что не сломаны соседние пути: прогрессивный fallback, выбор качества, resume из истории, субтитры. Проверка: ручной прогон на реальном Kodi, результат отмечен в `design.md`
